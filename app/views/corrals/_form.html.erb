<%= form_for(@corral) do |f| %>
  <% if @corral.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@corral.errors.count, "error") %> prohibited this corral from being saved:</h2>

      <ul>
      <% @corral.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :agreement %><br>
    <%= f.text_area :agreement %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
